<section class="card">
    <header>
      <h2>{{title}}</h2>
      <p>Posted by {{creator_id}} on {{createdAt}}</p>
      </header>
      <div>
        <p>
          {{content}}
        </p>
      </div>
</section>